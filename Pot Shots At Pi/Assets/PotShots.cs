using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Graph : MonoBehaviour
{
    //imagine being named "Dart" lmao
    public Transform dart;

    public int resolution = 100;
    //use ? for ternary operation 
    private float circledarts;
    private float squaredarts;

    void Awake()
    {
        
    }

    void Update(){
        //use time.wizard to auto write program 
        //use srand(time(NULL)) to generate random numbers 
        
        if ((50 * Time.framecount) < resolution)){
            for (int i =0; i<50; i++)
            {

            }

        }


        
    }
